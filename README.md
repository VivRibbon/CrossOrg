# CrossOrg
A Python script to take in a .puz crossword file and export it in a manner compatible with Emacs Org-Mode, to allow solving puzzles in Org without having to format them manually. Can write the puzzle to a new Org file or append it to an existing one if the destination name given exists.
